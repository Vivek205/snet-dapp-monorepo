import React from "react";
import PropTypes from "prop-types";

const MobileHeader = ({ MobileHeader }) => {
  return <MobileHeader />;
};

MobileHeader.propTypes = {
  MobileHeader: PropTypes.elementType,
};

export default MobileHeader;
