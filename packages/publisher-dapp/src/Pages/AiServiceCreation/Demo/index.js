import React from "react";
import { withStyles } from "@material-ui/core/styles";

import { useStyles } from "./styles";

const Demo = ({ classes, location }) => {
  return <span>demo</span>;
};
export default withStyles(useStyles)(Demo);
