import React from "react";
import { withStyles } from "@material-ui/core/styles";

import { useStyles } from "./styles";

const PricingDistribution = ({ classes, location }) => {
  return <span>PricingDistribution</span>;
};
export default withStyles(useStyles)(PricingDistribution);
