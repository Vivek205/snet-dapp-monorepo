import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
  increaseTopSpace: { marginTop: 105 },
  addMarginTopToVerticalTab: {
    "& .MuiPaper-root": { top: 111 },
  },
  addMarginTop: { marginTop: 40 },
});
